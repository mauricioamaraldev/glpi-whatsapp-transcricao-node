services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: glpi-whatsapp-producao
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
