services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: glpi-whatsapp-local
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: development
    volumes:
      - ./src:/app/src:ro
